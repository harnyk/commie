# Create a taskfile.yaml file
version: 3

tasks:
  dev:
    cmds:
      - go run .
  dev-install:
    cmds:
      - rm -rfv dist
      - goreleaser --snapshot
      - cp dist/commie_linux_amd64_v1/commie ~/bin
